def text_bits():

    print()
    var_text = input("Enter some text ")
    var_length = len(var_text)
    num_nits = 8 * var_length

    print()
    print("\'{}\' has {} characters ...".format(var_text, var_length))
    